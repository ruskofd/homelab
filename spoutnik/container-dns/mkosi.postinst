#!/usr/bin/env bash

set -euxo pipefail

# disable stub resolver
tee /etc/systemd/resolved.conf << END
[Resolve]
DNSStubListener=no
END

systemctl enable systemd-networkd \
                  systemd-resolved
