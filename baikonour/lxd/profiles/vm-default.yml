---
config:
  limits.cpu: "1"
  limits.memory: 1024MiB
  security.secureboot: "false"
  cloud-init.user-data: |
    #cloud-config
    ssh_authorized_keys:
      - ssh-ed25519 <>
description: Default LXD profile for virtual machines
devices:
  config:
    source: cloud-init:config
    type: disk
  eth0:
    name: eth0
    nictype: bridged
    parent: lxdbr0
    type: nic
  root:
    path: /
    pool: ssd-zfs
    type: disk
name: vm-default
used_by: []
